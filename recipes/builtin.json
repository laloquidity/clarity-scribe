[
  {
    "id": "spotify.play",
    "describe": "Search Spotify and play the top result",
    "verified": "2026-07-21 — ran live on Spotify desktop (Windows): deep link opened search, Play resolved in the accessibility tree and was invoked, window title changed to the requested track",
    "patterns": [
      "play {query} on spotify",
      "open spotify and play {query}",
      "spotify play {query}"
    ],
    "slots": ["query"],
    "source": "builtin",
    "steps": [
      { "action": "deeplink", "uri": "spotify:search:{{query}}" },
      { "action": "waitFor", "target": { "nameFromSlot": "query" }, "timeoutMs": 15000 },
      { "action": "click", "target": { "name": "Play", "type": "Button", "invokable": true } }
    ]
  }
]
